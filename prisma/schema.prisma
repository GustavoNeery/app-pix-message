generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Participant {
  id                String @id @default(uuid())
  nome              String
  cpfCnpj           String
  ispb              String
  agencia           String
  contaTransacional String
  tipoConta         String
}
